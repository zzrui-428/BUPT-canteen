.section1{
margin-bottom: 30rpx;
margin-top: 30rpx;
font-size: 30px;
}
.section{
margin-bottom: 20rpx;
margin-top: 20rpx;
font-size: 30px;
}
.form{

width:750rpx; /* class为form的组件宽度为750rpx，即屏幕宽度*/

height:1000rpx; /*高度为1000rpx*/

display:flex; /*组件的子组件采用flex布局排列*/

flex-direction:column; /*flex布局排列的主方向为纵向，子组件从上到下排列*/

justify-content:space-around; /*子组件在主方向上采用space-around方式排列，每个子组件在主方向上的两侧有相等的间隔。*/

align-items:center /*子组件在副方向（这里为横向）上居中排列*/

}

.input{

width:85%; /*class为input的组件宽度为父组件的宽度的85%*/

}

.input-box{

border: 1px solid black; /*给class为input-box的组件加上边框，边框宽度为1px，边框样式为实线，边框颜色为黑色。*/
margin-top: 80rpx /*此组件上方增加80rpx的间隔*/

}

.checkbox{

width:85%; /*class为checkbox的组件宽度为父组件的宽度的85%*/
}

.checkbox_hint{

margin-bottom:80rpx /*此组件下方增加80rpx的间隔*/
}

.section1{
margin-bottom: 30rpx;
margin-top: 30rpx;
font-size: 30px;
}
.sectionTitle{
  font-size: 80rpx;
  color:rgb(255, 138, 102);
  font-weight: bold;
  text-align: center;
  text-shadow: 0rpx 2rpx 4rpx  #ccc;
  font-family:SimHei,Microsoft JhengHei;  
  margin-top: 30rpx;

   
}

/*普通样式*/
.stepper .normal {
color:black;
}
/*禁用样式*/
.stepper .disable {
color: #ccc;
}
.about-banner{
height: 300rpx;
width: 300rpx;
border-radius: 5%;
margin:40rpx;
box-shadow: 0rpx 4rpx 10rpx #ccc;
}

.section1{
margin-bottom: 30rpx;
margin-top: 30rpx;
font-size: 30px;
}
.section{
margin-bottom: 20rpx;
margin-top: 30rpx;
font-size: 30px;
}

.menu{
margin-top:80rpx;
display: flex;
margin-left: 40rpx;
font-size:50rpx

}

.menu-details{
display: flex;
flex-direction: column
}

.menu-image{
width: 400rpx;
height: 266rpx;
margin-right:40rpx;
margin-top:40rpx;
margin-bottom:40rpx
}

.form{

width:675rpx;

height:650rpx; 

align-items: center;

display:flex; /*组件的子组件采用flex布局排列*/

flex-direction:column; /*flex布局排列的主方向为纵向，子组件从上到下排列*/

justify-content:space-around; /*子组件在主方向上采用space-around方式排列，每个子组件在主方向上的两侧有相等的间隔。*/

}

.input{

width:85%; /*class为input的组件宽度为父组件的宽度的85%*/

}

.input-box{

height:80rpx;
border: 1px solid rgba(0, 0, 0, 0.1) ; /*给class为input-box的组件加上边框，边框宽度为1px，边框样式为实线，边框颜色为黑色。*/
border-radius: 40rpx;

}

.checkbox{

width:85%; /*class为checkbox的组件宽度为父组件的宽度的85%*/
}

.checkbox_hint{

margin-bottom:80rpx /*此组件下方增加80rpx的间隔*/
}

.button{
text-align: center;
background-color: #FF8C69;
color: white;
border-radius: 40rpx;
width:580rpx;
height:90rpx;
box-shadow: 0px 2px 5px #ccc;
line-height: 90rpx;
font-family:Haettenschweiler, ‘ArialNarrowBold’, sans-serif;

}
.button-next{
background-color: rgb(218, 90, 51);
color: white;
margin-right: 20rpx;
font-size: 60rpx;
width:100%;
box-shadow: 0px 2px 5px #ccc;
}
.section1{
margin-bottom: 30rpx;
margin-top: 30rpx;
font-size: 30px;
}

.stepper {
width:80px;
height:26px;
margin: 0 auto;
border: 1px solid #ccc;
border-radius: 5px;
}
/*加号和减号*/
.stepper text {
float: left;
width: 19px;
line-height: 26px;
text-align: center;
}
/*数值*/
.stepper input {
width: 40px;
height: 26px;
float: left;
margin: 0 auto;
text-align: center;
font-size: 12px;
border-left: 1px solid #ccc;
border-right: 1px solid #ccc;
}
/*普通样式*/
.stepper .normal {
color:black;
}
/*禁用样式*/
.stepper .disable {
color: #ccc;
}
.shadow{
background-color: #ffffff;
display: inline-block;
margin:30rpx;
border-radius:20rpx;
padding: 10px;
box-shadow: 0rpx 4rpx 15rpx #ccc;
}